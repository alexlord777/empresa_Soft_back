import  express  from "express";
const proy= express.Router();



proy
     .get("/",)
     .get("/:poyectID",)
     .post("/",)
     .patch("/:proyectID")
     .delete("/:proyectsID")

export default proy;